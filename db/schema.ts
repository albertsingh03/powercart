import { sqliteTable, text, integer, index, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const products=sqliteTable('products',{key:text('key').primaryKey(),owner:text('owner').notNull(),data:text('data').notNull(),version:integer('version').notNull().default(0),lastOp:text('last_op'),identityKey:text('identity_key')},t=>[index('products_owner').on(t.owner),uniqueIndex('products_owner_identity').on(t.owner,t.identityKey)]);
export const events=sqliteTable('events',{id:text('id').primaryKey(),owner:text('owner').notNull(),productKey:text('product_key').notNull(),kind:text('kind').notNull(),at:integer('at').notNull(),data:text('data').notNull()},t=>[index('events_owner_at').on(t.owner,t.at)]);
export const receipts=sqliteTable('receipts',{id:text('id').primaryKey(),owner:text('owner').notNull(),filename:text('filename').notNull(),objectKey:text('object_key').notNull(),mime:text('mime').notNull(),at:integer('at').notNull(),note:text('note').notNull().default('')},t=>[index('receipts_owner').on(t.owner)]);
